[project]
name = "chengdu-edu-intel"
version = "0.1.0"
requires-python = ">=3.12"

[tool.uv.workspace]
members = [
  "packages/core",
  "packages/storage",
  "packages/collectors",
  "packages/parsers",
  "packages/scheduler",
  "packages/api",
]

[tool.pytest.ini_options]
testpaths = ["packages"]
asyncio_mode = "auto"

[dependency-groups]
dev = [
  "pytest>=8.0",
  "pytest-asyncio>=0.24",
  "httpx>=0.27",
  "ruff>=0.8",
]
